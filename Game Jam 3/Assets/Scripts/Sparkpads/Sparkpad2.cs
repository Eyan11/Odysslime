using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//when a Spark Slime triggers this Sparkpad, set  
//GameObject "Falling Platform" > Constraints > Freeze Rotation > X 
//to "false"

public class Sparkpad2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
